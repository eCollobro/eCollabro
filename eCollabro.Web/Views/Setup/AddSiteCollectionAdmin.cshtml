@using eCollabro.Web.Base;
@using eCollabro.Client.Models.Core
@using eCollabro.Client.Models
@model SiteCollectionAdminModel
@using (Html.BeginForm("SiteCollectionAdmin", "Setup", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", id = "frmAddSiteCollectionAdmin" }))
{
    <div class="modal fade" id="divAddSiteCollectionAdmin">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                    <h4 class="modal-title">Add- Site Collection Admin</h4>
                </div>
                <div class="modal-body">
                    @Html.AntiForgeryToken()
                    <div class="alert-info">Please enter your user name.</div>
                    <hr />
                    <div id="divSummaryMessageAddSiteCollectionAdmin"></div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.UserName, new { @class = "col-md-4 control-label" })
                        <div class="col-md-8">
                            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", maxlength = 20})
                            @Html.ValidationMessageFor(m => m.UserName)
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary" ng-click="saveSiteCollectionAdmin()">Save</button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->
}
